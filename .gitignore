# Working documents. Word keeps lock and recovery files beside them.
*.docx
~$*
~WRL*.tmp

# Citizen audit forms exported from KoboToolbox. build_map.py reads them for the
# audit layers, but they stay unpublished: they hold unvalidated rows, Kobo IDs
# and photo links. The page carries only the cleaned, validated points.
data/*.csv

# Reports and working PDFs, including MOD Foundation's BSF report.
*.pdf

# Analysis material: QGIS project, classification rasters, figures, screenshots.
*.png
*.pptx
*.qgz
classification/
cloudnative/
make_word_table.py

# The retired write-up page.
_archive/

# Local working instructions.
CLAUDE.md

# Python
__pycache__/
*.pyc
.venv/
